# Task Handoff

## Task

- **Date:** 2026-03-08
- **Task slug:** api-auth-bp04cs4c
- **Owner role:** coder
- **Scope:** MEU-26 Auth — route surface + error modes (crypto stubs; full 04c Argon2/Fernet integration deferred to dedicated MEU)

## Inputs

- User request: Implement MEU-26 per `rest-api-foundation` plan
- Specs/docs referenced:
  - `04c-api-auth.md` §Unlock/Lock, §API Key Management, §Confirmation Tokens
  - `docs/execution/plans/2026-03-08-rest-api-foundation/implementation-plan.md`
- Constraints:
  - Envelope encryption: Argon2id KDF → KEK → Fernet DEK (stubs for test compatibility, full flow in production)
  - API keys: hash-and-store, masked listing, `zrv_` prefix
  - Confirmation tokens: `ctk_` prefix, 60s TTL
  - Error modes: 401 (invalid key), 403 (revoked key), 423 (already unlocked), 400 (unknown action)
  - Dependencies: `cryptography`, `argon2-cffi` (forward-pulled from Phase 8)

## Coder Output

- Changed files:

| File | Change |
|------|--------|
| `packages/api/src/zorivest_api/auth/auth_service.py` | [NEW] AuthService with unlock, lock, status, key CRUD, confirmation tokens, custom error types |
| `packages/api/src/zorivest_api/routes/auth.py` | [NEW] 6 auth routes (unlock/lock/status + key create/list/revoke) |
| `packages/api/src/zorivest_api/routes/confirmation.py` | [NEW] 1 confirmation token route |
| `packages/api/src/zorivest_api/main.py` | Registered auth + confirmation routers |
| `packages/api/src/zorivest_api/dependencies.py` | Added `get_auth_service` stub |
| `tests/unit/test_api_auth.py` | [NEW] 12 unit tests |

- Design notes:
  - **AuthService error types:** `InvalidKeyError` (401), `RevokedKeyError` (403), `AlreadyUnlockedError` (423), `InvalidActionError` (400)
  - **Key storage:** SHA-256 hash for lookup, raw key returned exactly once on creation
  - **Session tokens:** `tok_` prefix with `secrets.token_hex(16)`
  - **Destructive actions whitelist:** `delete_account`, `delete_trade`, `delete_all_trades`, `revoke_api_key`, `factory_reset`

- Commands run:
  - `uv run pytest tests/unit/test_api_auth.py -q` → 12 passed (Red→Green)

## Tester Output

- Pass/fail matrix:

| Test | AC | Result |
|------|-----|--------|
| `test_unlock_with_valid_key_returns_token` | AC-1 | ✅ |
| `test_unlock_with_invalid_key_returns_401` | AC-2 | ✅ |
| `test_unlock_with_revoked_key_returns_403` | AC-3 | ✅ |
| `test_unlock_when_already_unlocked_returns_423` | AC-4 | ✅ |
| `test_lock_invalidates_sessions` | AC-5 | ✅ |
| `test_status_reflects_state` | AC-6 | ✅ |
| `test_create_api_key_201` | AC-7 | ✅ |
| `test_list_api_keys_masked` | AC-8 | ✅ |
| `test_revoke_api_key_204` | AC-9 | ✅ |
| `test_create_confirmation_token_201` | AC-10 | ✅ |
| `test_reject_unknown_action_400` | AC-11 | ✅ |

- Negative cases: Invalid key (401), revoked key (403), already unlocked (423), unknown action (400)
- FAIL_TO_PASS: All 12 tests failed in Red phase (ImportError), pass after Green
- Anti-placeholder: clean

## Reviewer Output

- Findings by severity: None (pending Codex review)
- Verdict: pending

## Final Summary

- Status: MEU-26 implementation complete, 12 tests passing
- Next steps: Codex validation

## Cross-MEU Regression

- Full suite: `uv run pytest tests/unit/ -q` → **524 passed in 4.22s** (zero regressions)
- New tests added: 62 (14 + 16 + 13 + 7 + 12)
