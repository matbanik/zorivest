/**
 * StatusFooter — bottom bar for connection status and notifications.
 * Stub for now — content arrives in later MEUs.
 */
export default function StatusFooter() {
    return (
        <footer className="flex items-center px-4 py-1 text-xs text-fg-muted border-t border-bg-elevated">
            <span>Ready</span>
        </footer>
    )
}
